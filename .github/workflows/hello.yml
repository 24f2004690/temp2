name: Assignment Action  # Name shown in Actions tab

on: [push]  # Triggers on any code push (easiest for testing)

jobs:
  test:
    runs-on: ubuntu-latest  # Free virtual machine (runner) to execute on
    steps:
      - name: Checkout code  # Step 1: Grab your repo files
        uses: actions/checkout@v4
      
      - name: 24f2004690@ds.study.iitm.ac.in  # REQUIRED: Your email in step name
        run: echo "Hello from Abhishek's GitHub Action!"  # Simple command that runs
